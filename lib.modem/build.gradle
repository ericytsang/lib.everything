dependencies {
    compile (project(":lib.regulatedstream"))
    compile (project(":lib.simplepipestream"))
    compile (project(":lib.net"))
    compile (project(":lib.onlycallonce"))
    testCompile (project(":lib.concurrent"))
}
