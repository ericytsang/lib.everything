configureAndroidLib(project)

android {
    defaultConfig {
        minSdkVersion 22
    }
}

dependencies {
    api project(":androidlib.core")
    api project(":lib.prop")
    api project(":lib.xy")
}
