apply plugin: "kotlin"

dependencies {

    // kotlin
    implementation deps.kotlin.stdlib
    implementation deps.kotlin.reflect

    api deps.rxjava2

    // test
    testImplementation project(":lib.testutils")

    // project dependencies
    api project(":lib.awaitable")
}
